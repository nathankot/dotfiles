" Explicitly source the base stuff that works even with `--noplugin`
so ~/.vim/plugins.vim
so ~/.vim/core.vim
so ~/.vim/keymap.vim
so ~/.vim/status_line.vim
so ~/.vim/tab_line.vim
color nk256

" @see ./plugin/*.vim for other stuff that automatically
" gets loaded by vim
